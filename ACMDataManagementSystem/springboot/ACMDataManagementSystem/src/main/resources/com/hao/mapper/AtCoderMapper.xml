<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hao.mapper.AtCoderMapper">
    <select id="contestList" resultType="com.hao.pojo.AcContest">
        select id, contest_id, contest_name, begin_time, length, url, create_time, update_time
        from ac_contest
        <where>
            <if test="contestId != null">
                contest_id like concat('%', #{contestId}, '%')
            </if>
            <if test="contestName != null">
                contest_name like concat('%', #{contestName}, '%')
            </if>
            <if test="beginTime">
                begin_time like concat('%', #{beginTime}, '%')
            </if>
        </where>
        order by begin_time desc
    </select>
    <select id="problemList" resultType="com.hao.pojo.AcProblem">
        select id, problem_id, problem_name, problem_rate, url, create_time, update_time
        from ac_problem
        <where>
            <if test="problemId != null">
                problem_id like concat('%', #{problemId}, '%')
            </if>
            <if test="problemName != null">
                problem_name like concat('%', #{problemName}, '%')
            </if>
            <if test="problemRate != null">
                problem_rate like concat('%', #{problemRate}, '%')
            </if>
        </where>
        order by problem_id
    </select>
</mapper>
